class Solution(object):
    def rotate(self, matrix):
        print(matrix)
        n = len(matrix)
        for i in range(n):
            for j in range(i+1):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
        print(matrix)

        for row in matrix:
            row.reverse()
        
